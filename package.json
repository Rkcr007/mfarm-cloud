{
  "name": "mfarm-cloud",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "workers/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present && npm run test:deploy",
    "test:deploy": "node --test deploy/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "backup:drill": "./deploy/restore-drill.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "@types/ws": "^8.5.0",
    "lucide-static": "^1.40.0",
    "typescript": "^5.7.0"
  }
}
